---

netdata_required_packages:
  - hddtemp
  - smartmontools

netdata_ansible_selinux_packages:
  Debian:
    - python-selinux
    - policycoreutils-python-utils
  RedHat:
    - libselinux-python
    - policycoreutils-python

netdata_package_uri: null

netdata_master: "{{ netdata_master_host }}"
netdata_master_host:
netdata_stream_uuid:

netdata_sysconfdir:
  Debian: /etc/default
  RedHat: /etc/sysconfig

...
